websockets>=14.0,<16.0
aiohttp>=3.9,<4.0
# openwakeword is installed separately with --no-deps (see Dockerfile/README).
# Its metadata requires tflite-runtime, for which Python 3.11/aarch64 has no
# suitable wheel; this project uses the ONNX path only.
numpy>=1.24,<2.0
onnxruntime>=1.16,<2.0
scipy>=1.10,<2.0
scikit-learn>=1.3,<2.0
requests>=2.28,<3.0
tqdm>=4.60,<5.0
# microwakeword runtime dependencies. pymicro-features ships aarch64 Linux
# wheels and pure-source for macOS. tflite-runtime historically has patchy
# wheel availability across (python, arch) combos, so the code prefers it
# but falls back to ai-edge-litert (Google's actively maintained successor).
# Both packages are opt-in — omit them entirely on machines that only use
# KWS_RUNTIME=openwakeword.
pymicro-features>=1.0; sys_platform == "linux"
ai-edge-litert>=1.0; sys_platform == "linux"
